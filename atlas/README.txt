# Planetary Atlas

A lightweight disk atlas model for protoplanetary disks.

It can:
- Find hidden planet candidates.
- Identify missing-physics zones.
- Classify disk types.

Main code lives in `atlas/disk_atlas.py`.
Run `run_atlas.py` after wiring in your data arrays.
